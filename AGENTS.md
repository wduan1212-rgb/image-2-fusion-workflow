# AGENTS.md - AI视频提示词工作台开发指令

## 0. 项目定位
本项目不是“替用户完整创作视频内容”的工具，而是“AI视频提示词骨架搭建工作台”。

系统负责提供可选的结构化骨头；用户负责填写具体内容内脏。

- 骨头：参考图多选开关、视频基础参数、风格类型与风格变量、角色设定、声音、镜头结构、机位/景别/运镜、负面约束、输出格式。
- 内脏：具体画面内容、产品信息、场景、剧情、卖点、口播文案、品牌信息、特殊要求。

所有模块都必须可选，不得假设每条视频都有角色、产品、口播、BGM或参考图。

## 1. 网站包含两个一级模块

### 1.1 自由搭配视频提示词工作台
这是当前主模块。目标是让用户通过选择变量和填写镜头内容，生成结构稳定、可复制的AI视频提示词。

核心结构：
1. 参考图片多选开关（无参考图不输出文本；有参考图/参考角色图等用 @ 标签输出）
2. 视频基础信息
3. 风格类型 + 风格变量树
4. 角色设定变量（可选）
5. 声音变量（可选）
6. 镜头结构变量
7. 每个镜头的内容输入和机位/景别/运镜设置
8. 负面约束标签
9. 输出格式

### 1.2 GPT image-2 快捷提示词模块
这是独立的图片提示词模块，不要和视频工作台混写。

image-2 模块方向：
- 商业海报
- 产品概念图
- 旅行海报
- 教育/试卷类
- 包装设计提案
- 安装说明图/教程图
- 其他图像模板

image-2 模块的生成逻辑应是：图片类型 -> 版式 -> 主体/产品 -> 风格 -> 色彩 -> 文案/文字要求 -> 画面细节 -> 负面约束。

当前实现已接入常用图片提示词库，按 12 个分类承载 120 条模板，并将每 10 条模板的拼接预览图以 base64 嵌入页面。image-2 工作区顺序应保持为：分类纵览图片 -> 变量表单 -> 模板卡片 -> 负面约束与自定义变量。

## 2. 全局设计原则

### 2.1 骨架固定，骨头可选，内脏用户填
提示词最终顺序固定：

{@参考图 @参考角色图，可选；无参考图时不输出任何参考图文本}
这是一条【比例】、【时长】秒的【风格类型】视频，风格为【风格变量】。
【角色设定，可选】
【声音锚点，可选】
【镜头结构】
Shot 1（时间）：【用户填写镜头内容】，【景别/角度】，【运镜】。
Shot 2（时间）：【用户填写镜头内容】，【景别/角度】，【运镜】。
……
【负面约束】

用户可以增删镜头、调整每个镜头秒数。系统只保证结构，不替用户决定具体内容。

### 2.2 变量调用必须短
变量进入最终提示词时，只调用“变量名称”或极短标准描述。不要把长篇解释塞进最终提示词。

示例：
- 亚洲外观 -> “亚洲外观”
- 真实iPhone实拍感 -> “真实iPhone实拍感”
- 平视中景 -> “平视中景”
- 手持轻微跟拍 -> “手持轻微跟拍”

详细说明只放在“查看详细”里。

风格变量必须挂在“风格类型”下形成变量树；风格类型同步显示在顶部摘要和实时预览中。风格变量库允许用户新增自定义风格变量，自定义变量进入“自定义风格”分类。

### 2.3 变量使用元素树，不做无脑平铺
角色设定不是平行标签堆叠，而是树：

角色设定
- 角色开关：无角色 / 单角色 / 多角色 / 仅手部 / 仅背影
- 角色外貌
  - 性别
  - 年龄
  - 人种/族裔外观
  - 长相风格
  - 脸部细节
  - 身材体型
  - 发型发色
- 角色穿搭
  - 服饰风格
  - 配饰
  - 鞋包
- 角色一致性约束

镜头结构也是树：

镜头结构
- 总时长
- Shot 列表
  - 时间段
  - 用户画面内容
  - 景别
  - 角度
  - 运镜
  - 单镜头负面约束（可选）

参考图也是可选多选开关：
- 无参考图：最终提示词开头不输出“无参考图”或“参考图设置”。
- 有参考图：输出 `@参考图`。
- 参考角色图：输出 `@参考角色图`。
- 参考产品图：输出 `@参考产品图`。
- 参考风格图：输出 `@参考风格图`。

“有口播”不属于参考图片开关，不应出现在提示词最开始的参考图区域。

### 2.4 冲突只提醒，不禁止
系统可以提示冲突，但用户可以继续生成。

常见提醒：
- 无角色 + 人物口播
- 无BGM + BGM氛围
- 无字幕 + 用户填写字幕需求
- 一镜到底 + 多镜头Shot
- 真实实拍 + 强CG风格
- 仅手部出镜 + 面部近景

### 2.5 转场变量暂不作为核心模块
不要在主工作台中强制加入复杂转场。转场更适合后期剪辑，复杂转场会影响AI视频画面稳定。

如果保留入口，只作为“高级可选/后期建议”，默认关闭。

## 3. 数据结构建议

### 3.1 Variable
```ts
type Variable = {
  id: string;
  name: string;
  category: string;
  output: string;        // 进入最终提示词的短文本
  detail: string;        // 查看详细里的简洁解释
  tags?: string[];
  recommendFor?: string[];
  cautionWith?: string[];
};
```

### 3.2 Shot
```ts
type Shot = {
  id: string;
  start: number;
  end: number;
  userContent: string;
  framing?: Variable;
  angle?: Variable;
  cameraMove?: Variable;
  soundLine?: string;
  negativeTags?: Variable[];
};
```

### 3.3 PromptProject
```ts
type PromptProject = {
  referenceModes: Array<'reference_image' | 'character_reference' | 'product_reference' | 'style_reference'>;
  aspectRatio: '9:16' | '16:9' | '1:1' | '4:5';
  duration: number;
  styleType?: string;
  style?: Variable;
  customStyles?: Variable[];
  character?: CharacterConfig | null;
  sound?: SoundConfig | null;
  shots: Shot[];
  globalNegativeTags: Variable[];
  outputLanguage: 'zh' | 'en' | 'bilingual';
};
```

## 4. 最终提示词拼接规则

### 4.1 中文输出模板
```text
{referenceTokens，可选}
这是一条{aspectRatio}、{duration}秒的{styleType}视频，风格为{style}。
{characterText}
{soundText}

【镜头结构】
{shotsText}

【负面约束】
{negativeText}
```

### 4.2 Shot 输出模板
```text
Shot {index}（{start}-{end}秒）：{userContent}。{framing}{angle}{cameraMove}
```

注意：userContent 是用户填写内容，系统不得自动过度扩写。

## 5. 变量文案规范

- output 字段必须短，通常 2-12 个汉字或一个短短语。
- detail 字段控制在 1-2 句，不写长篇适用场景。
- 不要在每个变量里重复“适合视频类型”“不适合场景”“负面约束”等长说明；这些用于规则层，而不是变量层。
- 变量名称应该可读、可复用、可搜索。

## 6. 用户体验要求

- 工作台用左侧变量区 + 中间Shot结构区 + 右侧预览区。
- Shot 可以新增、删除、复制、拖拽排序。
- 每个 Shot 秒数可调，但总时长必须实时校验。
- 负面约束是标签类，多选，可取消。
- 角色模块、声音模块、参考图模块都可开关。
- 参考图模块支持多个参考入口同时选择，并用 @ 标签进入最终提示词。
- 风格变量库允许新增、删除、调用用户自定义变量。
- 生成结果必须一键复制。
- 提供“简洁模式”和“详细模式”：简洁模式只输出骨架和变量名；详细模式加入必要短描述。

## 7. 禁止事项

- 不要强制每条视频有角色。
- 不要强制每条视频有产品。
- 不要强制每条视频有口播/BGM。
- 不要把查看详细内容塞进最终提示词。
- 不要默认开启复杂转场。
- 不要把变量设计成过细动作库。
- 不要替用户写完整剧情或卖点，除非用户明确要求。

## 8. 当前开发优先级
1. 搭建自由搭配视频提示词工作台。
2. 实现元素树变量选择。
3. 实现可增删调秒数的Shot结构。
4. 实现可选角色模块、声音模块、负面约束模块。
5. 实现最终提示词生成和复制。
6. image-2 快捷提示词模块保持独立，变量表单紧跟分类纵览图片，模板卡片位于变量表单下方。
